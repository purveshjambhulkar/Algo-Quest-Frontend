
import { UserStats } from "@/types";

// Empty data for initialization
export const defaultUserStats: UserStats = {
  totalSolved: 0,
  easy: 0,
  medium: 0,
  hard: 0,
  streak: 0,
  lastPracticed: null
};
